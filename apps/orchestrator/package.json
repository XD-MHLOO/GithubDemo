{
  "type": "module",
  "name": "orchestrator",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "clean": "rimraf dist",
    "prebuild": "pnpm run clean",
    "build": "nest build",
    "start:prod": "node dist/apps/orchestrator/src/main",
    "dev": "dotenv -e ../../.env.dev -- nest start --watch"
  },
  "dependencies": {
    "@githubdemo/libraries": "workspace:*"

  }
}
